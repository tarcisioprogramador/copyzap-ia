{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:web": "pnpm --filter @workspace/copyzap run dev",
    "dev": "concurrently \"pnpm run dev:api\" \"pnpm run dev:web\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:production": "pnpm run typecheck:libs && pnpm --filter @workspace/copyzap run build && pnpm --filter @workspace/api-server run build",
    "start:production": "pnpm run db:push && node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "db:push": "pnpm --filter @workspace/db run push",
    "db:studio": "pnpm --filter @workspace/db run studio",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "setup": "pnpm install && pnpm run docker:up && echo '\n✅ Setup completo! Execute: pnpm run db:push && pnpm run dev'"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
